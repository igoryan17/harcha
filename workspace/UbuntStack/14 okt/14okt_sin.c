#include <pthread.h>
#include <math.h>
#include <stdio.h>

// integrate thread num 10k

int f1(int thread_num, int razb )
{
	int val; //количество
	int i;
	float pi = 3.1415926;
	float x, dx, s = 0.0; // s - площадь


		scanf("%d %d \n", thread_num, razb);

		val = (thread_num, razb);

	for(i = 0; i < razb; i++)
		printf("%d \n", i);
	return NULL;
}
